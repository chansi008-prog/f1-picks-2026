# 아빠 vs 딸 그랑프리 픽 — F1 패밀리 게임

## 게임 규칙

- 아빠와 딸, 두 사람이 하는 게임.
- 2026 F1 시즌의 매 그랑프리마다: **레이스**에서 각자 드라이버 2명을 고른다. **스프린트가 있는 라운드**(중국·마이애미·캐나다·영국·네덜란드·싱가포르)는 스프린트에서도 각자 2명을 고른다.
- 고른 드라이버 2명이 그 세션에서 실제로 획득한 F1 포인트의 **합**이 그 사람의 그 세션 점수.
- 세션마다 더 높은 합산 점수를 낸 사람이 "라운드 승리".
- 시즌 내내 각자의 누적 포인트, 누적 라운드 승수를 비교한다.

## 포인트 규정 (공식 F1 규정과 동일)

- 레이스: 1~10위 → 25, 18, 15, 12, 10, 8, 6, 4, 2, 1점. 11위 이하 0점.
- 스프린트: 1~8위 → 8, 7, 6, 5, 4, 3, 2, 1점. 9위 이하 0점.
- 패스티스트랩 보너스 없음 (2025년부터 폐지).
- DNF / DNS / DSQ는 모두 0점.

## 드라이버 번호 (driver_number 기준, OpenF1 API와 동일한 번호 체계)

시즌 중 로스터 변경이 있었음: 12라운드(네덜란드 GP)부터 **유키 츠노다(#22)**가 레이싱불스로 합류, **리암 라우슨(#30)**이 한 라운드 레드불 대타로 출전한 적 있음(이후 다시 레이싱불스). 새 앱에서는 매 라운드 실제 참가자 목록을 그때그때 API로 받아오는 게 안전함 (고정 명단을 하드코딩하면 이런 변경에 취약).

## 지금까지의 픽 데이터

`f1-picks-2026.json` 파일에 1~14라운드 픽이 정리되어 있음 (같은 디렉토리에 첨부). 구조:
```json
{
  "<round>": {
    "race":   { "dad": [driver_number, driver_number], "daughter": [driver_number, driver_number] },
    "sprint": { "dad": [...], "daughter": [...] }   // 스프린트 있는 라운드만
  }
}
```
이 파일을 시드 데이터로 그대로 가져다 쓰면 됨.

## 기술적으로 겪었던 시행착오 (중요 — 새로 만들 때 반복하지 말 것)

1. **Claude 아티팩트 샌드박스는 외부 API `fetch()`를 막는다.** 그래서 처음엔 결과를 실시간으로 못 가져오고, 매 라운드 Claude가 공식 F1 결과 페이지를 확인해서 수동으로 앱에 박아 넣는 방식으로 운영했음. → **일반 iOS 앱(Claude 밖)에서는 이 제약이 없으므로, OpenF1 API(`https://api.openf1.org/v1/`)를 앱이 직접 실시간으로 호출하면 된다.** 엔드포인트: `/meetings?year=2026`, `/sessions?year=2026`, `/session_result?session_key=`, `/drivers?session_key=`, `/championship_drivers?session_key=`, `/championship_teams?session_key=`.
2. **팀(컨스트럭터) 순위는 요약 페이지를 그대로 믿지 말 것.** Formula1.com의 팀 순위 페이지가 한 번 캐시가 오래되어 틀린 값을 준 적이 있음. 라운드별 개별 결과에서 직접 합산해서 검증하는 로직을 넣는 걸 추천.
3. **픽 저장은 `window.storage`(Claude 전용 기능)로 되어 있었는데, 이건 일반 앱에서는 못 쓴다.** 새 앱에서는 Firebase Realtime Database(또는 유사한 무료 백엔드)로 대체 필요. REST로 간단히 GET/PUT 하는 방식이면 충분 (SDK 없이도 가능).
4. **아빠·딸 픽을 공유하려면 Claude 계정을 같이 쓰면 안 된다** (Anthropic 약관 위반). 새 앱은 각자 로그인 없이도, 같은 공유 DB 주소만 앱에 설정하면 픽이 서로 보이는 구조가 간단하고 충분함 (가족 단위 소규모 사용이라 정교한 인증까지는 불필요).
5. 이미 만들어둔 PWA 프로토타입 파일(index.html, manifest.json, sw.js, 아이콘)이 있음 — 필요하면 참고용으로 요청해서 받을 수 있음. 다만 그건 Claude 안에서 만든 초안이라 실제 배포·테스트는 못 해본 상태.

## 원하는 최종 형태

- 아이폰에서 쓰는 네이티브 또는 앱에 가까운 형태.
- 위 게임 규칙대로 라운드별 픽 선택 UI, 자동 채점, 시즌 누적 스코어보드, 드라이버·팀 순위, 포인트 추이 그래프.
- 아빠·딸 두 기기에서 픽이 서로 보이는 공유 저장.
